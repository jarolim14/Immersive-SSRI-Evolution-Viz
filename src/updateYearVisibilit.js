import { getCurrentYear } from "./yearSlider.js";

// Somewhere in your code where you need the current year
function someFunction() {
  const selectedYear = getCurrentYear();
  console.log("The currently selected year is:", selectedYear);
  // Use selectedYear in your logic...
}
